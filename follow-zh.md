# 跟随显示场景

（程序界面默认跟随系统语言；下面以中文说明。）

## 一句话说明

「跟随」让一个显示场景动态继承另一个显示场景的**全部布局和绑定**，两者的差异只有：**名字**，以及**绑定的物理显示器**。源场景一改，跟随它的场景下次切换时自动同步——你不用再改第二遍。

## 为什么会有这个功能

设想你在公司用两块横屏显示器，已经精心配好了一整套场景：浏览器在左、编辑器在右、聊天工具固定一角……一堆绑定。

回到家，你也是两块横屏显示器。在你眼里这"本质就是同一套工作场景，只是换了两台物理显示器"。但按常规做法，你得为"家庭"这个新场景把每个绑定**从头再配一遍**，以后公司那边一改，家里还得手动跟着改。

「跟随」就是为消除这份重复而生的。

## 跟随 ≠ 复制

- **复制**是一次性快照：把公司的配置抄一份给家里。抄完两者各走各的；公司之后改了，家里**不会**跟着变，会逐渐"漂移"，你得手动补。
- **跟随**是动态继承：家里始终实时继承公司的布局。公司改一个绑定，家里下次切换立刻就是新的。**一处维护，处处生效。**

## 它能帮你做什么

- 多套"同形态"的环境共用一套布局：家 / 公司、台式机 / 笔记本扩展坞、不同会议室的双屏……配一次，到哪都对。
- 同一个快捷键，在不同硬件下都落到正确的显示器上——gmux 会按你当前接的实际显示器自动选对场景。

## 和普通显示场景相比，用起来有什么不同

这是使用跟随场景时最该记住的几点：

1. **布局不在跟随场景里改。** 跟随场景没有自己的绑定列表——要调整布局，请到它**所跟随的源场景**上改，改完两边一起变。
2. **跟随场景里只能改这几样：** 名字、绑定的显示器、显示器的角色映射（本场景的每块屏"扮演"源场景的哪块屏）、以及改跟随对象。
3. **形状必须兼容才能跟随：** 显示器**数量相同**、且每块的**朝向一致**（横对横、竖对竖）。数量或朝向对不上的场景不会出现在可跟随列表里。
4. **想单独改一两个绑定？转为独立。** 跟随场景的编辑页提供「转为独立显示场景」：把当前继承来的全部绑定固化成这个场景自己的配置，从此不再跟随，你就能随意单独调整了。
5. **被跟随的源场景不能直接删。** 只要还有场景在跟随它，删除会被拦下，并提示你先把那些场景「转为独立」或改其跟随对象。
6. **跟随失效时不会乱摆窗口。** 如果你后来改了源场景的显示器数量或朝向，导致映射对不上，那个跟随场景会在列表里标红提示你更新映射；在它修好之前，gmux 会跳过它而不是把窗口摆错地方。

## 怎么用

1. 新建或编辑一个显示场景，选好它绑定的显示器。
2. 展开「高级」。
3. 在"布局来源"里选「跟随已有显示场景（自动同步布局）」。
4. 选择要跟随的源场景（只会列出形状兼容的）。
5. 核对显示器映射——本场景的每块屏扮演源场景的哪块屏（gmux 会按朝向自动配好默认值，可微调）。
6. 保存。完成后，这个场景就会持续跟随源场景的布局。
