# 管理者として実行

> **言語** · [中文](run-as-admin-zh.md) · [English](run-as-admin-en.md) · **日本語**

← [ホームに戻る](ja.md)

---

## 2つのスイッチ

**設定 → システム → 起動** カードに2つのチェックボックスがあります：

| スイッチ | 動作 |
|---------|------|
| **起動時に管理者として実行** | gmux を開くたびに UAC プロンプトが表示され、確認後に管理者権限で実行されます |
| **自動起動時に管理者として実行** | ログオン時に管理者権限で gmux が起動します（Windows タスクスケジューラ経由 — UAC プロンプトなし） |

2つは完全に独立しており、一方をオン・オフしてももう一方は変わりません。既定では「自動起動時に管理者として実行」がオン（タスクスケジューラ経由のため UAC プロンプトなし）、「起動時に管理者として実行」がオフです。

## なぜ管理者権限が必要か

Windows には **UIPI**（ユーザーインターフェイス特権分離）というセキュリティ機構があり、低い権限のプロセスは高い権限のプロセスのウィンドウを操作できません。

具体的には、gmux が通常権限で動いていて、別のアプリが管理者権限で動いている場合（レジストリエディタ、一部のゲームのアンチチート、一部の開発ツールなど）、gmux は以下のことができません：

- そのウィンドウの**移動やリサイズ**（`SetWindowPos` が UIPI によりブロック）
- そのウィンドウの**アクティブ化やフォーカス**
- UAC 昇格が必要なアプリの**起動**

この状況では、gmux は該当リージョンにゴールドの枠線警告を表示します：「xxx は管理者として実行中 — gmux はウィンドウを管理できません」。

## 管理者として実行すると何が解決するか

gmux 自体が昇格して実行されると、どのアプリよりも権限レベルが低くならないため、UIPI の制限がなくなります：

- すべてのウィンドウを正常に移動・リサイズ・アクティブ化できます
- 昇格が必要なアプリも問題なく起動できます
- 権限不足の警告が表示されなくなります

## トレードオフ

- 手動起動のたびに **UAC プロンプト**が表示されます（タスクスケジューラ経由の自動起動ではプロンプトなし）
- **セキュリティ**：管理者権限は gmux（および起動する子プロセス）により広いシステムアクセスを与えます。gmux は子プロセスを自動的に降格（de-elevate）しますが、すべてのケースで保証されるわけではありません
- 常用アプリに管理者権限が不要な場合、この機能を有効にする必要はありません
