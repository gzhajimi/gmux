# 管理员身份运行

> **语言** · **中文** · [English](run-as-admin-en.md) · [日本語](run-as-admin-ja.md)

← [回到首页](zh.md)

---

## 两个开关

在 **设置 → 系统 → 启动** 卡片里有两个开关：

| 开关 | 作用 |
|------|------|
| **启动时使用管理员身份** | 每次打开 gmux 时自动弹出 UAC 提示，确认后 gmux 以管理员权限运行 |
| **开机自启时使用管理员身份** | 开机自启时以管理员权限启动 gmux（通过 Windows 任务计划程序实现，不会弹 UAC） |

两者完全独立、互不影响：开启或关闭其中一个，不会改变另一个。默认情况下，"开机自启时使用管理员身份"是开启的（走任务计划程序，不弹 UAC），而"启动时使用管理员身份"是关闭的。

## 为什么需要管理员权限

Windows 有一套叫 **UIPI**（用户界面特权隔离）的安全机制：低权限进程不能操控高权限进程的窗口。

具体来说，如果 gmux 以普通权限运行，而你的某个应用以管理员权限运行（比如注册表编辑器 regedit、某些游戏反作弊、部分开发工具），gmux 就无法：

- **移动或调整**该窗口的大小（`SetWindowPos` 被 UIPI 拦截）
- **激活或聚焦**该窗口
- **启动**需要 UAC 提权的应用

遇到这种情况时，gmux 会在对应区域显示一个金色边框提示："xxx 以管理员权限运行，gmux 无法管理其窗口"。

## 以管理员身份运行 gmux 能解决什么

当 gmux 自己也以管理员权限运行时，它的权限等级不低于任何应用，UIPI 限制消失。这意味着：

- 所有窗口都能正常移动、调整大小、激活
- 需要提权的应用也能正常启动
- 不再出现权限不足的警告提示

## 有什么代价

- **每次手动启动**都会弹出 UAC 确认对话框（开机自启走任务计划程序，不会弹）
- **安全性**：管理员权限意味着 gmux（以及它启动的子进程）拥有更高的系统访问权限。gmux 启动子进程时会自动降权（de-elevate），让子进程以普通权限运行，但这并非所有场景都能保证
- 如果你的所有常用应用都不需要管理员权限，就没必要开启这个功能
